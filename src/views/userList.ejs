<html>
<%- include('./partials/header.ejs') %>

    <body>
        <h1>User List</h1>
        <ul>
            <% for(let i=0; i < users.length; i++) { %>
                <li>
                    <a href="/users/detail?userId=<%=users[i].id%>"> <%= users[i].name %> </a> 
                </li>  
                <% } %>
        </ul> 
    </body>

</html>